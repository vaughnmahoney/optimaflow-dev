import React from "react";
import { Layout } from "@/components/Layout";
import Supervisor from "./Supervisor";

// We're renaming the route but keeping the functionality from Supervisor page
const Attendance = () => {
  return <Supervisor />;
};

export default Attendance;
