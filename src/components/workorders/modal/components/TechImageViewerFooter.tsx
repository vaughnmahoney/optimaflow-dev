
import React from 'react';

interface TechImageViewerFooterProps {
  // No props needed since we're removing the button
}

export const TechImageViewerFooter = ({}: TechImageViewerFooterProps) => {
  return (
    <div className="bg-white border-t px-4 py-3"></div>
  );
};
